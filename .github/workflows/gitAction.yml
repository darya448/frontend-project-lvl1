name: eslint-actions
on: push
jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      -name: npm install
        -run: make install
      -name: Run Linter 
       - run: make lint